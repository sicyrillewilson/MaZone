package tg.eplcoursandroid.mazone.operations

import tg.eplcoursandroid.mazone.entites.Ticket

class TicketOperation {
    /*private val tickets = mutableListOf<Ticket>()

    fun addTicket(ticket: Ticket) {
        tickets.add(ticket)
    }

    fun getAllTickets(): List<Ticket> = tickets

    fun getTicketsByUser(userId: Int): List<Ticket> {
        return tickets.filter { it.userId == userId }
    }*/
}